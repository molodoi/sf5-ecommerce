{% extends 'base.html.twig' %}

{% block title %}Nos produits{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-3">
            <h1 class="h2">Filtrer</h1>
            {{ form(searchForm) }}
        </div>
        <div class="col-9">
            <div class="row">        
                {% for product in products %}
                    {% include 'product/single_product.html.twig' %}
                {% endfor %}
            </div>
        </div>
    </div>    
{% endblock %}